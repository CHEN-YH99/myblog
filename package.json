{
  "name": "my-blog-system",
  "version": "1.0.0",
  "description": "统一后端API的全栈博客系统",
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:frontend\" \"npm run dev:admin\"",
    "dev:api": "cd myblog && npm run dev:server",
    "dev:frontend": "cd myblog && npm run dev",
    "dev:admin": "cd myblog-admin && npm run dev",
    "build": "npm run build:frontend && npm run build:admin",
    "build:frontend": "cd myblog && npm run build",
    "build:admin": "cd myblog-admin && npm run build",
    "install:all": "npm install && cd myblog && npm install && cd ../myblog-admin && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "vue3",
    "typescript",
    "express",
    "mongodb",
    "blog",
    "cms"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@types/multer": "^2.0.0",
    "marked": "^16.2.1",
    "multer": "^2.0.2",
    "tsx": "^4.20.5"
  }
}
