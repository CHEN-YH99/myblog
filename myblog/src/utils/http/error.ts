/**
 * ============================================================
 * HTTP 错误处理 - 前台 (myblog) 重新导出
 * ============================================================
 *
 * 此文件是 shared/utils/http/error 的重新导出。
 * 前台项目直接使用共享的错误处理实现，无需额外包装。
 *
 * 使用方式：
 * ```ts
 * import { HttpError, handleError, showError } from '@/utils/http/error'
 * ```
 */

export * from '@shared/utils/http/error'
